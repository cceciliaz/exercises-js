const numero = [10, 20, 30, 40, 50]
// retorna a posicao e quantidade dos elementos
for(let i = 0; i < numero.length; i++){
    console.log(i);
}
// retorna os elementos em si
for(let i = 0; i < numero.length; i++){
    console.log(numero[i]);
}
// esses acima sao for normais


// for off que vai direto no elemento em si 
for(o off objetos){
    console.log(o);
}  // aqui é um exemplo ele foi em todos os elementos que estava na variavel objetos so nao entendi o porque do "o" ele é como uma variavel??
